pipeline{
    agent any
    stages{
        stage('checkout'){
            steps{
                
                dir( Ec2 ){
                    checkout scmGit(branches: [[name: '*/main']], extensions: [], userRemoteConfigs: [[url: 'https://github.com/Harikrishna241/Terraform.git']])
                    sh 'ls -al'
                }
                
            }
        
        }
    }
}